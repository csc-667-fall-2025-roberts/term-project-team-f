<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <title>Error <%= status %> - Bullshit</title>
  </head>
  <body>
    <div class="error-page">
      <div class="error-container">
        <div class="error-icon <%= status === 404 ? 'not-found' : (status >= 500 ? 'server-error' : 'forbidden') %>">
          <% if (status === 404) { %>
            🔍
          <% } else if (status >= 500) { %>
            💥
          <% } else if (status === 403) { %>
            🚫
          <% } else { %>
            ⚠️
          <% } %>
        </div>

        <div class="error-code"><%= status %></div>
        
        <h1 class="error-title">
          <% if (status === 404) { %>
            Page Not Found
          <% } else if (status === 403) { %>
            Access Denied
          <% } else if (status >= 500) { %>
            Server Error
          <% } else { %>
            Something Went Wrong
          <% } %>
        </h1>

        <p class="error-message">
          <%= message %>
        </p>

        <div class="error-actions">
          <a href="/" class="btn btn-primary">
            🏠 Go Home
          </a>
          <a href="/lobby" class="btn btn-secondary">
            🎮 Go to Lobby
          </a>
        </div>

        <% if (stack) { %>
          <div class="error-stack">
            <pre><%= stack %></pre>
          </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
